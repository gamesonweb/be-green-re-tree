class ItemView {
    constructor(itemModel, itemController) {
      this.itemModel = itemModel;
      this.controller = itemController;
      this.init();
    }
  
    init() {
      // Render the available items
      // Add event listeners to buy items and attach them to the controller
    }
  }
  